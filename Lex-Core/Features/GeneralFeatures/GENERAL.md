# General Feaatures
- the application should use the mediator patterns and CQRS and be completely decoupled from a potential ui. the app's core class library should be completely self-contained and be able to be connected to a CLI or a UI based on future decisions.
- the apps core library should handle all database operations and have a mediatr based Entry point for the UI to access and use the app.
